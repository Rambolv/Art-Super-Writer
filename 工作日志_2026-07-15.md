# 工作日志 — 2026-07-15

> 今日会话涉及代码审查、Bug 修复、功能开发、文档撰写、项目打包等多项工作。

---

## 目标任务

1. 审查 Stateless 模式在所有 LLM 调用入口的受控情况
2. 修复写作方向提示词存储路径问题
3. 新增「全自动长篇写作」占位页面
4. 新增 SKILL 导入功能
5. 创建项目 EXE 启动器
6. 创建绿色便携版打包脚本
7. 撰写完整中英双语文 README（多轮迭代）

---

## 实际执行

### 1. Stateless 模式审查

- 追踪了所有 LLM 调用入口（callLLM/callLLMStream/session.send 等共 9 个入口）
- 确认了以下入口的模式受控情况：
  - ✅ analyzeProject（分析按钮）— 通过 _callViaSession 受控
  - ✅ writeChapter（写作台）— 显式判断 profile.workMode
  - ⚠️ analyzeChapterContent（分析本章）— 部分受控，用了 writer 角色的 profile 而非 analysis
  - ✅ analyzeSkills（技能学习）— 受控
  - ✅ 审查室（startReview/callReviewer/computeSqueezeLLM）— 受控
  - ❌ testConnection（测试连接）— 不受控（影响不大）
- 发现 analyzeChapterContent 缺少全文分批阅读机制，只发新章节内容

### 2. 写作方向存储路径修复

- **问题：** 方向提示词保存在 `projectLog/v{N}/`，依赖 `analysisCount` 版本号；加载用 `readFileFromDiskLatest` 查找，分析版本为 0 时保存到 v1 但加载只搜 v0
- **修复：** 将保存和加载路径改为 `chapters/writer_direction_第XXX章.json`，与章节文件放在一起
- 旧文件从 `projectLog/v1/` 迁移到 `chapters/`
- 加载逻辑：chapters/ → 回退 projectLog 旧位置

### 3. 新增「全自动长篇写作」页面

- 左侧导航栏添加 🤖 全自动长篇写作 按钮
- 中间面板显示支付宝收款二维码 + 提示文字
- 从 `C:\AI\AITOOLS\vsr_removecharacter_windows_gpu_v1.1.0\` 复制了收款码图片

### 4. SKILL 导入功能

- 右侧 SKILL 面板新增 📥 导入 按钮
- 支持格式：`.txt` `.md` `.docx` `.xlsx` `.json`
- 引入 CDN 库：mammoth.js（Word 解析）、SheetJS（Excel 解析）
- Word 文档自动提取纯文本，Excel 提取全部表格为 CSV

### 5. EXE 启动器

- 用 PowerShell + Windows Forms 编写启动器 GUI
- 使用 ps2exe 编译为 `超逸写手启动器.exe`
- 支持勾选：启动服务器 / 打开主页 / 启动本地 LLM
- 本地 LLM 选项自动检测 `LLM/` 文件夹是否存在
- 路径全部使用相对路径

### 6. 绿色便携版打包

- 编写 `build_portable.py` 打包脚本
- 生成 `Art-Super-Writer-Portable.zip`（4.7MB）
- 包含首次安装依赖脚本 + 日常启动脚本

### 7. README 文档（共迭代 9 个版本）

最终版本覆盖 29 个要点：
- 中英文完整分页（锚点切换）
- 通俗聊天文风
- 优势劣势诚实对比表
- 30 秒快速开始
- 两种写作方式（已有文档 / 新开）
- 写作台每个按钮逐一详解
- Skill 系统 + 导入
- 审查室完整流程
- 世界树 / 角色池 / 节奏控制台 / 历史日志
- 设置页：独立指定 LLM 策略（🔥 核心省钱功能）
- Slot vs Stateless 原理解释
- 本地 LLM 部署：Qwen3.6-35B-A3B 越狱版 MoE 架构
- GPU/CPU 分层策略（--n-cpu-moe 999）
- 4 种量化选项菜单 + 完整参数表
- 多模态支持（--mmproj）
- 模糊记忆 / 版本选择器 / SKILL 自动学习
- 项目文件夹结构
- FAQ

### 8. GitHub 提交记录

| 提交 | 内容 |
|------|------|
| `c645a3d` | 写作方向存储重构 + 全自动长篇写作页面 |
| `09ed4dd` | SKILL导入 + EXE启动器 + 写作方向修复 |
| `89dcafb` | README v1: 新手超详细指南 |
| `71e8e3a` | README v2: 写作台按钮逐一说明 |
| `9f2ad87` | README v3: 补全所有页面 |
| `26af806` | README v4: 优劣势表开头+30秒快速开始 |
| `a035ac9` | README v5: Slot优势+本地LLM省钱 |
| `45a9e99` | README v6: 中英双语文档+打包脚本 |
| `67734b7` | README v7: 中英分页+Slot推荐+DeepSeek支持 |
| `e4ee5ee` | README v8: 18项要求逐一落实 |
| `c0eaa5f` | 修正参数解释 |
| `4ca1d95` | MoE优化方案精准重写 |
| `a77b733` | 实际模型名+4种量化菜单+独立LLM策略 |

---

## 成果 vs 目标

| 目标 | 结果 |
|------|------|
| Stateless 模式审查 | ✅ 完成，发现 analyzeChapterContent 需改造 |
| 写作方向路径修复 | ✅ 已修复并迁移旧文件 |
| 全自动长篇写作页面 | ✅ 已添加 |
| SKILL 导入功能 | ✅ 支持 5 种格式 |
| EXE 启动器 | ✅ 已编译 |
| 绿色便携版打包 | ✅ 生成 4.7MB ZIP |
| README 文档 | ✅ 9 次迭代，覆盖 29 个要点 |

---

## 遗留问题

- [ ] analyzeChapterContent 需添加全文分批阅读
- [ ] analyzeChapterContent 的 profile 应从 writer 改为 analysis 角色
- [ ] Stateless 模式下多轮对话重发完整历史浪费 Token（analyzeProject/analyzeSkills）
- [ ] 审查室 Slot 模式适配
